# 小任务 13：双窗口接入与旧入口替换

**文档创建时间：** 2026-09-17T14:01:39+08:00

**状态：** 未开始

**所属总任务：** [工作台标题栏与全部任务卡片页](./README.md)

**复杂度：** 4/5；窗口层级、任务切换、草稿、归档和查看确认存在异步交叉。

**依赖：** 11、12

## 范围与非目标

接入浏览器与 Electron 标题栏并替换旧下拉；不改节点树渲染与运行控制语义。

## 已完成事实

- 用户已确认交互方向；本任务尚未实现。
- 现有入口与旧版执行记录见总任务，不重复实施旧版工作。

## 剩余步骤

- [ ] 将标题栏全部任务入口连接所属工作台卡片页，覆盖节点树但保留标题栏，避免盖住其他工作台。
- [ ] 接入标题栏/卡片切换，保持草稿、分支语义、树订阅生命周期；成功显示结果后才发送查看确认。
- [ ] 处理已有抽屉、输入区域与卡片页的焦点/遮挡关系，避免覆盖后的底层控件仍可误操作。
- [ ] 统一当前任务归档、归档过程中用户切换、无剩余任务、任务加载失败和窗口销毁处理。
- [ ] 移除被替代的标题栏下拉与专属无用代码，保留其他仍有调用的历史入口；更新受影响测试和权威说明。
- [ ] 执行定向跨模块回归，汇总最终验证入口，不启动图形界面。

## 定向自动验证

覆盖浏览器/Electron 调用路径、草稿保持、窗口间隔离、归档竞态、迟到加载不误标已查看及订阅释放。 实施时根据实际变更登记可执行命令、退出码与关键断言，不将未运行的检查记为通过。

## 完成标准

新入口接入完成，旧下拉无残留调用，定向测试及类型检查通过；进入 08。 人工体验仅在最后综合验证中执行；完成后回写总任务并按规范删除本文件。

