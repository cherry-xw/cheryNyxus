# 小任务 12：全部任务卡片页

**文档创建时间：** 2026-09-17T14:01:39+08:00

**状态：** 未开始

**所属总任务：** [工作台标题栏与全部任务卡片页](./README.md)

**复杂度：** 3/5；搜索、稳定排列和页面恢复涉及多步骤异步交互。

**依赖：** 10、11

## 范围与非目标

实现覆盖节点树的卡片页；保持当前工作台范围，不新增全局任务中心。

## 已完成事实

- 用户已确认交互方向；本任务尚未实现。
- 现有入口与旧版执行记录见总任务，不重复实施旧版工作。

## 剩余步骤

- [ ] 构建等高卡片网格与固定搜索筛选区；卡片同时展示最近要求和最新进展/结果，不放树缩略图或编造摘要。
- [ ] 接入全历史搜索、命中片段、状态/时间/排序；显示结果数、清除筛选、加载更多、空态及失败重试。
- [ ] 打开页面后保持卡片位置，实时仅更新内容；新任务显示待刷新提示，不符合当前筛选的原卡片暂留到刷新。
- [ ] 普通打开恢复上次条件和滚动；通过角标进入时应用对应筛选；关闭不清空原节点树位置。
- [ ] 点击卡片请求切换任务；更多菜单支持标题栏显示/收起、归档；卡片浏览和 tip 不标记结果已查看。
- [ ] 完善键盘进入/退出、Escape 关闭、焦点恢复与可读状态文案；补充列表状态与请求处理测试。

## 定向自动验证

覆盖迟到搜索结果、筛选组合、分页重复、后台更新不重排、恢复位置、加载失败与归档失败。 实施时根据实际变更登记可执行命令、退出码与关键断言，不将未运行的检查记为通过。

## 完成标准

卡片页状态与数据测试通过；页面可由两类窗口共同接入。 人工体验仅在最后综合验证中执行；完成后回写总任务并按规范删除本文件。

