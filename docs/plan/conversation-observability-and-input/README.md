# 上下文统计、待办进度与连续消息交互

**文档创建时间：** 2026-09-18T00:56:28+08:00

**状态：** 规划中

## 目标与边界

为后续需求讨论保留有实现依据的设计起点，覆盖上下文统计、To-do list 进度、连续消息的并入和展示。面向节点树、对话模式、精简模式，统一分析问题、容易出错的情况、交互设计和建设顺序。

本次交付是[设计初稿与待细化事项](01-design-draft.md)，不是已确认的产品规则或详细实施方案；不修改产品代码，不删除上下文入口。初稿完成不代表功能完成，总任务保持“规划中”。文中的建议可在后续讨论中调整。

## 批次与小任务台账

当前只展开 01；其余为后续候选工作，待需求范围确定后再建立独立小任务文件。复杂度是当前估计，执行前按实际范围重新评估，并确认执行 Agent。

| 批次 | 小任务 | 状态 | 复杂度及依据 | 依赖 |
| --- | --- | --- | --- | --- |
| A | [01 初稿讨论与详细需求细化](01-design-draft.md) | 进行中 | 3：需核对消息流程、三种展示模式与统计定义 | 初稿已形成；待后续讨论 |
| B | 02 统一统计和输入状态的接口说明 | 未开始 | 4：涉及请求身份、持久记录、恢复与旧数据 | 01 确认相关规则 |
| C | 03 上下文统计采集与详情模块 | 未开始 | 4：实际请求、模型返回数据与历史对比跨多个模块 | 02；先采集再画图 |
| C | 04 待办进度统一读取与三模式展示 | 未开始 | 3：复用工具记录，处理作用范围、历史和更新失败 | 02 中待办相关规则 |
| C | 05 连续消息采用策略与三模式展示 | 未开始 | 4：并发到达、停止、重试和恢复需保持一致 | 02 中输入相关规则 |
| D | 06 综合验证与用户验收 | 未开始 | 3：核对三项功能联动、多窗口和异常恢复 | 03—05 完成后建立清单 |

03—05 会修改共同的工作台、会话状态和协议入口，不能仅因同处批次 C 就同时修改这些文件。优先完成稳定的数据读取规则，再分别接入展示。

## 依赖与恢复检查点

- 已完成事实：已阅读模块文档并核对项目源码；初稿给出现状依据、推荐方向、备选方案、异常情况及后续验证目标。未做真实界面验收，也未向模型服务发起请求验证统计能力。
- 当前工作：01 的初稿已交付，详细需求尚未开始；下一次从初稿第 8 节的问题逐项讨论，再按确定范围拆分实施。
- 工作区基准：本次检查时存在大量前端、服务和其他计划的未提交修改；本文以当时工作区为准，继续实施前重新核对相关文件，不覆盖原有改动。
- 相邻计划：[标题栏与全部任务卡片页](../workbench-session-strip/README.md)。本任务复用其工作台和会话切换方向，不重新规划标题栏或替换节点树。
- 后续稳定规则归属：消息与恢复规则进入[共享时间线说明](../../shared/architecture/canonical-timeline.md)及[共享协议](../../shared/protocol/README.md)；展示进入[前端模块](../../frontend/README.md)；实现入口由[后端模块](../../backend/README.md)维护。当前提案仅保留在计划区。
- 下一条文档检查命令：`pnpm plan:lint`；产品实施前再选择初稿第 7 节的定向验证入口。

## 最终综合验证与用户审批

当前仅检查计划索引、文档链接和修改范围。功能实施后的自动验证与人工操作集中到 06，届时建立操作卡；不提前登记产品测试通过，也不要求用户现在执行 UI 验收。

用户已要求基于现有实现形成详细初稿；消息默认采用策略、缓存指标范围、待办作用范围和上下文入口调整仍是待讨论建议。确认初稿方向不等于批准实施或通过最终验收。
