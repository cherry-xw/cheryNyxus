# S06 人物详情展示组件

**所属总任务：** [万象台](README.md)
**状态：** 未开始
**复杂度：** 简单，快速执行 Agent
**批次：** C
**步骤进度：** 0/4
**依赖：** [C15](15-C15-desktop-foundation.md)
**交接对象：** C17

## 目标

用已冻结展示模型呈现人物身份、任务、状态和基线。

## 输入与契约

- C15-view-v1 人物详情 props/emits
- S03 已完成样例（若未完成则使用 C15 自带样例）
- 必须使用前置任务已经交付的具体版本，不能根据字段名猜测尚未完成的协议。
- 后续发现契约变化先报告 C17，不自行改动共享类型。

## 允许修改的范围

- `web/src/features/wanxiangtai/components/InstanceDetails.vue 及同址专属样式`
- `web/test/wanxiangtai/instance-details.test.ts`
- 上述新路径为本任务专属目标；若前置契约指定了同职责等价路径，由总负责人记录映射后采用，不创建重复入口。

## 范围与非目标

- 显示团队、角色、实例编号、任务摘要、执行/等待原因、快照版本和详情入口。
- 仅维护展开、hover、焦点等局部状态；交互 emit 使用契约目标 ID，业务状态由 props 提供。
- 不调用 RPC/store、不定义休眠条件、不改应用装配和业务权限。
- 禁止修改数据库、公共协议、权限、业务状态机、RPC 注册和应用装配；禁止直接编辑总进度。
- 只实现明确完整产物，不新建只有透传用途的 container，不额外安装依赖。

## 执行步骤

- [ ] 1. 阅读指定输入与前置交接，记录契约版本，列出本任务产物和样例状态；缺失时报告交接对象。
- [ ] 2. 按范围完成「人物详情展示组件」，满足上方逐条内容要求，保持可独立消费。
- [ ] 3. 执行下方局部检查，修正文案、类型、样例或展示问题；业务契约问题升级给 C17。
- [ ] 4. 记录实际修改文件、检查结果与已知限制，交给 C17 复核并由总负责人同步总进度。

## 定向测试与检查

- 空字段、长名称、失败/等待、键盘聚焦及 emitted target 检查。
- 前端类型和静态检查，遵守主题、字重、字号及直角规则。

```text
pnpm exec vitest run --config web/vitest.config.ts web/test/wanxiangtai/instance-details.test.ts
pnpm web:type-check
```

测试入口是本任务将创建的文件，目前尚未存在。展示组件测试关注用户可见状态、键盘和事件目标，不重写业务判断。

## 完成标准

- 组件在无后端连接的样例环境可展示全部约定状态。
- 所有允许范围外变更均为零；四步有记录。
- 完成不等于整体前端或功能完成，由复杂任务接入真实业务后统一验收。

## 可直接派发给 Agent 的指令

> 执行 S06，先阅读本文件及前置契约。只修改“允许修改的范围”。按四步完成产物与局部检查，记录修改文件、结果和限制。发现契约、权限或业务规则缺失时交给 C17，不得自行补业务规则。不要提交 Git，不修改总计划，返回交接摘要。

## 协作与变更边界

- 执行前核对前置任务已完成且契约版本一致；当前状态“未开始”，未解锁不等于发生故障。
- 本次落盘不是实现批准或验收。遵循总文档的已确认需求；新增技术方案以“补全建议”提供，执行时不得冒充用户原话。
- 先更新对应模块文档，再实现代码。不得修改本总计划以外无关需求、不得代用户审批。
- 共享文件由总负责人分配；遇到权限、状态或契约变更需求提交给核心负责人，不同时覆盖他人文件。
- 每完成一个步骤更新本任务；由总负责人同步总计划的步骤统计，避免多 Agent 同写总文件。
- 最终回归发现问题时在父计划登记新的修正小任务，不恢复已删除任务，也不改写原完成摘要。

## 已完成事实与执行记录

仅完成本任务文档准备；产品实现、定向验证和交接均未开始。

| 项目          | 记录                         |
| ------------- | ---------------------------- |
| 执行 Agent    | 未分配                       |
| 开始/结束时间 | 未开始                       |
| 采用契约版本  | 未接收                       |
| 实际修改文件  | 无                           |
| 定向检查结果  | 未执行                       |
| 未解决问题    | 无已执行发现；前置条件见依赖 |
| 交接摘要      | 未交接                       |

---

## 评审调研结论（2026-09-12 整体评审落位）

> 来源：[整体评审与强化方案](review/design-review.md)、[可行性缺陷分析](review/feasibility-findings.md)、[技术栈调研](review/tech-stack-research.md)、[渲染栈聚焦调研](review/rendering-stack-research.md)。本节是评审产出的执行提示，只补充信息，不修改本任务既有范围、步骤与验收标准；标注 U-xx 的事项未经用户裁定前不得视为已确认需求。

### 1. 方案建议与调研结论建议
- 详情浮层定位采用官方 `@floating-ui/vue`（`useFloating`＋`arrow` middleware，≈32.7k），样式继续复用 pet 组件；浮层挂在会移动的人物上，不建议 DOM 内自绘跟随（渲染栈 §5；增-15 ③）。
- 身份显示用稳定 ID＋编号（团队／角色／实例编号），不做装饰性替换：显示名仅用于呈现，emit 与事件目标一律使用契约目标 ID（C15-view-v1 契约；S04 词表「显示名仅用于呈现」）。

### 2. 可能存在的问题点
- F-13 关联：详情身份字段应取 teamId／instanceId——C06 投影按 teamId／instanceId 判定身份与层级（增-14），不得以 `metadata.type` 推断，避免组长预设子会话被误读为普通「子 Agent」（feasibility §5 F-13；design-review §3 F-13、§5 增-14）。

### 3. 优化建议
- 增-15 ③：浮层定位统一走 `@floating-ui/vue`，样式层不改 pet 结构（渲染栈 §5.2）。
- 详情字段与 C06 投影字段对齐（三视图按稳定身份聚合），组件随对应业务链语义完成排入后再与 C17 交互连接（design-review §2.2 子系统⑫、§7.3）。
