# C08 回合调度与等待处理

**所属总任务：** [万象台](README.md)
**状态：** 未开始
**复杂度：** 复杂，核心实现 Agent
**批次：** C
**步骤进度：** 0/4
**依赖：** [C07](07-C07-discussion-storage.md)

## 目标

协调串行组长会议与并行团队执行，消除可识别的循环等待。

## 输入材料与事实核对

先读[完整设计记录](design-and-history.md) §2～§10 的有效方案，再读下列入口。标为前置产物的文件由前置任务创建；其他列出的入口用于定位现有实现，不代表本任务必须修改所有文件。

- `src/service/chat/wakeScheduler.ts`
- `src/service/chat/runtime.ts`
- `src/service/chat/send.ts`

## 范围与非目标

允许承担的修改范围：

- src/service/wanxiangtai/discussionScheduler.ts
- 万象台执行队列、等待关系与恢复记录
- 调度与等待事件投影

非目标：不顺手重构无关功能、不提前实现下游展示、不改用户项目原始文件来验证计划。具体物理文件可遵循仓库归属规范拆分，但若扩大共享写入范围，先由总负责人重新分配。不得将任务文档内算法建议升级为全局规范；需要形成规范时落入 docs/standards/。

## 执行步骤

- [ ] 1. 实现单组长单活动讨论、邀请队列与全部参与者原子占用；未占齐不部分锁定成员，按入队顺序处理并显式取消失效邀请。
- [ ] 2. 实现回合响应集合、重复响应去重、轮数上限、邀请拒绝/取消和参与者失效；等待回合不占模型执行槽位。
- [ ] 3. 实现有类型的等待关系检查；组长等成员时仍可在安全边界处理相关澄清，用户取消走确定性控制路径，检测环后拒绝新增阻塞边并上报。
- [ ] 4. 验证 A↔B 交叉邀请、三方等待环、父等子且子问父、连续会议、参与者退役及人工审批长期等待。

## 定向测试

- 轮数上限不被当作死锁检测器。
- 角色参与会议仍不会把后台未完成工作错误标为完成。
- 同一 chat 不并发启动两个模型运行。

新增定向测试入口：`test/wanxiangtai/discussion-scheduler.test.ts`。使用现有 Vitest，不另引入测试框架。该文件为实施目标，目前尚未创建。

```text
pnpm exec vitest run test/wanxiangtai/discussion-scheduler.test.ts
pnpm type-check
```

定向测试必须验证对外行为及失败后果，不只复写实现细节。全量检查由批次与最终回归统一记录，不把局部通过当综合通过。

## 完成标准

- 每场讨论有明确终态或可见阻塞原因。
- 无部分占用导致的会议死锁。
- 四步均已完成，定向验证有真实结果；产物与模块文档一致。
- 交接实际文件、接口/数据版本、运行命令、已知限制和受影响下游。
- 失败时保留可追溯证据，不通过默认成功或忽略错误掩盖问题。

## 协作与变更边界

- 执行前核对前置任务已完成且契约版本一致；当前状态“未开始”，未解锁不等于发生故障。
- 本次落盘不是实现批准或验收。遵循总文档的已确认需求；新增技术方案以“补全建议”提供，执行时不得冒充用户原话。
- 先更新对应模块文档，再实现代码。不得修改本总计划以外无关需求、不得代用户审批。
- 共享文件由总负责人分配；遇到权限、状态或契约变更需求提交给核心负责人，不同时覆盖他人文件。
- 每完成一个步骤更新本任务；由总负责人同步总计划的步骤统计，避免多 Agent 同写总文件。
- 最终回归发现问题时在父计划登记新的修正小任务，不恢复已删除任务，也不改写原完成摘要。

## 已完成事实与执行记录

仅完成本任务文档准备；产品实现、定向验证和交接均未开始。

| 项目          | 记录                         |
| ------------- | ---------------------------- |
| 执行 Agent    | 未分配                       |
| 开始/结束时间 | 未开始                       |
| 采用契约版本  | 未接收                       |
| 实际修改文件  | 无                           |
| 定向检查结果  | 未执行                       |
| 未解决问题    | 无已执行发现；前置条件见依赖 |
| 交接摘要      | 未交接                       |

---

## 评审调研结论（2026-09-12 整体评审落位）

> 来源：[整体评审与强化方案](review/design-review.md)、[可行性缺陷分析](review/feasibility-findings.md)、[技术栈调研](review/tech-stack-research.md)、[渲染栈聚焦调研](review/rendering-stack-research.md)。本节是评审产出的执行提示，只补充信息，不修改本任务既有范围、步骤与验收标准；标注 U-xx 的事项未经用户裁定前不得视为已确认需求。

### 1. 方案建议与调研结论建议
- 自研调度器方向正确（t2 领域 1：六领域均无现成栈可整体套用）：借鉴 LangGraph checkpoint/interrupt/resume（等待状态可持久化重建）、Claude Agent Teams 邮箱/attempt 所有权、AG2「按轮选人」反用为「按轮必需回复者栅栏等齐」（业界无现成实现，必须自写）、MetaGPT 发布/订阅公共流。
- 死锁防线＝wait-for 图环检测：组间邀请、父等子、审批等待统一登记为有类型等待边，新增阻塞边前做 O(V+E) DFS 环检测，成环即拒绝并按 D23 逐级上报；轮数上限仅作活锁保险，不是死锁处理。

### 2. 可能存在的问题点
- F-02 实证：审批挂起期间无 chunk 喂狗，5min 后被等待子 chat 的 runtime 被 abort（`wake_on_timeout` 默认 false），迟到 resolve 无恢复路径，多层等待链会级联中止。
- 排期风险：本任务为实现复杂度最高任务（t1 §7／评审 §7.1 均建议保留最大缓冲）。

### 3. 优化建议
- 增-03：wait-for 图环检测落为协议级闸门（本任务步骤 3「有类型的等待关系检查」的明确化）。
- 增-04：等待图持久化重建，覆盖 peer 停等、会议等待与审批等待。
- 增-05 ①：审批挂起暂停该链看门狗与等待计时（挂起≠卡死，禁止伪造 chunk）；U-02／U-03 裁定后落参数（上帝位离线阈值、审批超时处置）。
