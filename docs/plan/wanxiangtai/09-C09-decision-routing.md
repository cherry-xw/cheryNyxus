# C09 共识、裁定与审批路由

**所属总任务：** [万象台](README.md)
**状态：** 未开始
**复杂度：** 复杂，核心实现 Agent
**批次：** C
**步骤进度：** 0/4
**依赖：** [C08](08-C08-discussion-scheduler.md)

## 目标

把讨论关闭、提案同意、有效决策和执行授权分开。

## 输入材料与事实核对

先读[完整设计记录](design-and-history.md) §2～§10 的有效方案，再读下列入口。标为前置产物的文件由前置任务创建；其他列出的入口用于定位现有实现，不代表本任务必须修改所有文件。

- `src/service/interaction/handler.ts`
- `src/service/approval/manager.ts`
- `src/db/question.ts`
- `src/core/security/rolePolicy.ts`

## 范围与非目标

允许承担的修改范围：

- src/service/wanxiangtai/decisions.ts 与向上澄清工具
- 既有 interaction 的必要类型/归属扩展
- 决策版本、确认与上报记录

非目标：不顺手重构无关功能、不提前实现下游展示、不改用户项目原始文件来验证计划。具体物理文件可遵循仓库归属规范拆分，但若扩大共享写入范围，先由总负责人重新分配。不得将任务文档内算法建议升级为全局规范；需要形成规范时落入 docs/standards/。

## 执行步骤

- [ ] 1. 实现提案 ID/版本、必需确认者、异议、撤回和替代关系；新版本不继承旧版本同意，主持人关闭会议不能生成共识。
- [ ] 2. 实现授权内调度者裁定和逐级上报；缺乏用户偏好依据、范围扩张和授权外问题直至用户处理，保留依据与裁定者。
- [ ] 3. 保留 ask_user_question 用户直达语义，新增向上澄清业务能力；安全审批复用现有链路，不让组长代答或通过会议扩大工具权限。
- [ ] 4. 验证伪造确认、旧版本同意、缺席/退出确认者、异议未消除、用户审批超时及未生效提案阻止依赖任务开工。

## 定向测试

- 有效决策不可被普通压缩摘要改写。
- 裁定明确记录其覆盖的议题、授权范围与理由。
- 待办显示决定后影响的工作和操作后果。

新增定向测试入口：`test/wanxiangtai/decisions.test.ts`。使用现有 Vitest，不另引入测试框架。该文件为实施目标，目前尚未创建。

```text
pnpm exec vitest run test/wanxiangtai/decisions.test.ts
pnpm type-check
```

定向测试必须验证对外行为及失败后果，不只复写实现细节。全量检查由批次与最终回归统一记录，不把局部通过当综合通过。

## 完成标准

- 只有一致通过或合法裁定才获得相应执行效力。
- 安全审批继续由用户处理。
- 四步均已完成，定向验证有真实结果；产物与模块文档一致。
- 交接实际文件、接口/数据版本、运行命令、已知限制和受影响下游。
- 失败时保留可追溯证据，不通过默认成功或忽略错误掩盖问题。

## 协作与变更边界

- 执行前核对前置任务已完成且契约版本一致；当前状态“未开始”，未解锁不等于发生故障。
- 本次落盘不是实现批准或验收。遵循总文档的已确认需求；新增技术方案以“补全建议”提供，执行时不得冒充用户原话。
- 先更新对应模块文档，再实现代码。不得修改本总计划以外无关需求、不得代用户审批。
- 共享文件由总负责人分配；遇到权限、状态或契约变更需求提交给核心负责人，不同时覆盖他人文件。
- 每完成一个步骤更新本任务；由总负责人同步总计划的步骤统计，避免多 Agent 同写总文件。
- 最终回归发现问题时在父计划登记新的修正小任务，不恢复已删除任务，也不改写原完成摘要。

## 已完成事实与执行记录

仅完成本任务文档准备；产品实现、定向验证和交接均未开始。

| 项目          | 记录                         |
| ------------- | ---------------------------- |
| 执行 Agent    | 未分配                       |
| 开始/结束时间 | 未开始                       |
| 采用契约版本  | 未接收                       |
| 实际修改文件  | 无                           |
| 定向检查结果  | 未执行                       |
| 未解决问题    | 无已执行发现；前置条件见依赖 |
| 交接摘要      | 未交接                       |

---

## 评审调研结论（2026-09-12 整体评审落位）

> 来源：[整体评审与强化方案](review/design-review.md)、[可行性缺陷分析](review/feasibility-findings.md)、[技术栈调研](review/tech-stack-research.md)、[渲染栈聚焦调研](review/rendering-stack-research.md)。本节是评审产出的执行提示，只补充信息，不修改本任务既有范围、步骤与验收标准；标注 U-xx 的事项未经用户裁定前不得视为已确认需求。

### 1. 方案建议与调研结论建议
- 审批 payload 扩展团队/实例归属维度，复用现有用户审批链（t2 领域 1／领域 6 交叠；无现成栈，按机制条目自研）。
- 迟到 resolve 仿 `resolveQuestionBatch` durable-answers 模式补恢复，不再落在已清除的 runtime 上。

### 2. 可能存在的问题点
- F-08：多团队审批涌入单一队列且每条独立 deadline，用户离线时批量静默拒绝，任务大面积失败/阻塞。
- F-02 关联：迟到审批落在已清除 runtime 上，无恢复路径（与本任务审批归属扩展同链路）。

### 3. 优化建议
- 增-05：审批批量列表＋按任务聚合＋紧急/普通分级；上帝位离线超阈值→存在等待用户事项的任务显式转 paused（确定性控制，恢复后既有审批继续有效）。
- U-02（评审组建议阈值 30 分钟＋分级）与 U-03（评审组建议超时转「升级提醒」保持挂起，R13「过期不转为允许」不稀释）裁定后回填。
