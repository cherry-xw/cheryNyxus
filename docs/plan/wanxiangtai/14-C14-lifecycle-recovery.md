# C14 生命周期与统一恢复

**所属总任务：** [万象台](README.md)
**状态：** 未开始
**复杂度：** 复杂，核心实现 Agent
**批次：** E
**步骤进度：** 0/4
**依赖：** [C06](06-C06-state-projection.md)、[C07](07-C07-discussion-storage.md)、[C08](08-C08-discussion-scheduler.md)、[C09](09-C09-decision-routing.md)、[C10](10-C10-context-management.md)、[C11](11-C11-deliverables.md)、[C12](12-C12-integration.md)、[C13](13-C13-publish.md)

## 目标

协调实例、讨论、工作区、纪元、待办和任务结束的恢复闭环。

## 输入材料与事实核对

先读[完整设计记录](design-and-history.md) §2～§10 的有效方案，再读下列入口。标为前置产物的文件由前置任务创建；其他列出的入口用于定位现有实现，不代表本任务必须修改所有文件。

- `src/service/chat/treeControl.ts`
- `src/service/chat/childControl.ts`
- `src/service/chat/runRecovery.ts`
- `src/service/config/roleLifecycle.ts`
- `src/service/restartCoordinator.ts`

## 范围与非目标

允许承担的修改范围：

- src/service/wanxiangtai/lifecycle.ts 与恢复装配
- 现有暂停/恢复/角色删除的必要万象台钩子
- 归档、退役与结束状态投影

非目标：不顺手重构无关功能、不提前实现下游展示、不改用户项目原始文件来验证计划。具体物理文件可遵循仓库归属规范拆分，但若扩大共享写入范围，先由总负责人重新分配。不得将任务文档内算法建议升级为全局规范；需要形成规范时落入 docs/standards/。

## 执行步骤

- [ ] 1. 实现运行状态、展示位置、可执行资格分别管理；休息室仅接收没有任何未完成责任且非当前对话位的空闲实例。
- [ ] 2. 实现暂停、取消、退出、归档、退役对等待边、会议席位、待办、模型槽位、磁盘预留与交付的影响；主动取消不等于安全回滚外部效果。
- [ ] 3. 实现启动恢复：配置/纪元→文件操作→组织→投递→讨论→待办→执行；结果不明副作用进入人工判断；实现调度者提议、用户确认结束。
- [ ] 4. 验证运行中被顶替仍可见、同角色跨团队不互顶、旧纪元不可恢复、重启等待审批、结束前未处理交付、归档与退役区别。

## 定向测试

- 视觉归档不删除历史、交付或工作区。
- 组长无退役权；用户结束后历史只读。
- 恢复不重复外部工具副作用，不复活已撤销权限的实例。

新增定向测试入口：`test/wanxiangtai/lifecycle-recovery.test.ts`。使用现有 Vitest，不另引入测试框架。该文件为实施目标，目前尚未创建。

```text
pnpm exec vitest run test/wanxiangtai/lifecycle-recovery.test.ts
pnpm type-check
```

定向测试必须验证对外行为及失败后果，不只复写实现细节。全量检查由批次与最终回归统一记录，不把局部通过当综合通过。

## 完成标准

- 无孤儿等待、错误复活或资源预留泄漏。
- 四步均已完成，定向验证有真实结果；产物与模块文档一致。
- 交接实际文件、接口/数据版本、运行命令、已知限制和受影响下游。
- 失败时保留可追溯证据，不通过默认成功或忽略错误掩盖问题。

## 协作与变更边界

- 执行前核对前置任务已完成且契约版本一致；当前状态“未开始”，未解锁不等于发生故障。
- 本次落盘不是实现批准或验收。遵循总文档的已确认需求；新增技术方案以“补全建议”提供，执行时不得冒充用户原话。
- 先更新对应模块文档，再实现代码。不得修改本总计划以外无关需求、不得代用户审批。
- 共享文件由总负责人分配；遇到权限、状态或契约变更需求提交给核心负责人，不同时覆盖他人文件。
- 每完成一个步骤更新本任务；由总负责人同步总计划的步骤统计，避免多 Agent 同写总文件。
- 最终回归发现问题时在父计划登记新的修正小任务，不恢复已删除任务，也不改写原完成摘要。

## 已完成事实与执行记录

仅完成本任务文档准备；产品实现、定向验证和交接均未开始。

| 项目          | 记录                         |
| ------------- | ---------------------------- |
| 执行 Agent    | 未分配                       |
| 开始/结束时间 | 未开始                       |
| 采用契约版本  | 未接收                       |
| 实际修改文件  | 无                           |
| 定向检查结果  | 未执行                       |
| 未解决问题    | 无已执行发现；前置条件见依赖 |
| 交接摘要      | 未交接                       |

---

## 评审调研结论（2026-09-12 整体评审落位）

> 来源：[整体评审与强化方案](review/design-review.md)、[可行性缺陷分析](review/feasibility-findings.md)、[技术栈调研](review/tech-stack-research.md)、[渲染栈聚焦调研](review/rendering-stack-research.md)。本节是评审产出的执行提示，只补充信息，不修改本任务既有范围、步骤与验收标准；标注 U-xx 的事项未经用户裁定前不得视为已确认需求。

### 1. 方案建议与调研结论建议
- 借鉴 LangGraph checkpoint 思想（t2 领域 1）：等待图、审批等待、「当前对话位」随恢复顺序七阶段（配置/纪元→文件操作→组织→投递→讨论→待办→执行）重建。

### 2. 可能存在的问题点
- F-02 重建侧：peer 停等/会议等待的跨重启恢复为全计划最高复杂度新机制（现状无恢复机制）。
- F-11：「当前对话位」未点名为系统库持久化字段，缺失则 D7 休息室规则重启后无从判定。
- F-15：磁盘预留记账粒度粗，崩溃后释放依赖恢复扫描。

### 3. 优化建议
- 增-04（等待图持久化重建）、增-05（审批挂起×看门狗×上帝位时效机制）、增-07（currentConversationTarget 列入实例记录必填字段，随「组织」阶段重建）落地。
- U-02 裁定的「离线转 paused」任务在恢复顺序「用户待办」阶段提供显式恢复入口。
- 资源预留释放验证项维持（F-15 维持「无资源预留泄漏」验证，可选增预留对账事件）。
