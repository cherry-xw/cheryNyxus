# S05 四类场景模板数据

**所属总任务：** [万象台](README.md)
**状态：** 未开始
**复杂度：** 简单，快速执行 Agent
**批次：** C
**步骤进度：** 0/4
**依赖：** [C16](16-C16-scenes-and-skins.md)
**交接对象：** C16／C17

## 目标

在成熟布局引擎之上填充静态场景配置。

## 输入与契约

- C16-scene-v1 模板格式、校验器及最小合法模板
- 必须使用前置任务已经交付的具体版本，不能根据字段名猜测尚未完成的协议。
- 后续发现契约变化先报告 C16／C17，不自行改动共享类型。

## 允许修改的范围

- `web/src/features/wanxiangtai/skins/scenes/ 下四类静态模板`
- 上述新路径为本任务专属目标；若前置契约指定了同职责等价路径，由总负责人记录映射后采用，不创建重复入口。

## 范围与非目标

- 按既有字段填写用户区、团队工位、讨论区、会议区和休息室，名称通过 S04 语义映射。
- 采用 C16 给定的区域约束和尺寸参数，不自行设计新的碰撞、运动或自适应规则。
- 不改模板 schema、布局算法、Canvas 引擎、动画计时或持久化。
- 禁止修改数据库、公共协议、权限、业务状态机、RPC 注册和应用装配；禁止直接编辑总进度。
- 只实现明确完整产物，不新建只有透传用途的 container，不额外安装依赖。

## 执行步骤

- [ ] 1. 阅读指定输入与前置交接，记录契约版本，列出本任务产物和样例状态；缺失时报告交接对象。
- [ ] 2. 按范围完成「四类场景模板数据」，满足上方逐条内容要求，保持可独立消费。
- [ ] 3. 执行下方局部检查，修正文案、类型、样例或展示问题；业务契约问题升级给 C16／C17。
- [ ] 4. 记录实际修改文件、检查结果与已知限制，交给 C16／C17 复核并由总负责人同步总进度。

## 定向测试与检查

- 四套模板通过 C16 校验器。
- 区域引用完整，最小/常规窗口通过既有布局检查；视觉结果统一转交 C18 最终验收，不作为本任务完成条件。

执行 `pnpm web:type-check` 及前置任务交付的词表/模板校验。静态数据无需为每个值新增镜像测试。

## 完成标准

- 真实模板可替换最小测试模板，不需修改引擎。
- 所有允许范围外变更均为零；四步有记录。
- 完成不等于整体前端或功能完成，由复杂任务接入真实业务后统一验收。

## 可直接派发给 Agent 的指令

> 执行 S05，先阅读本文件及前置契约。只修改“允许修改的范围”。按四步完成产物与局部检查，记录修改文件、结果和限制。发现契约、权限或业务规则缺失时交给 C16／C17，不得自行补业务规则。不要提交 Git，不修改总计划，返回交接摘要。

## 协作与变更边界

- 执行前核对前置任务已完成且契约版本一致；当前状态“未开始”，未解锁不等于发生故障。
- 本次落盘不是实现批准或验收。遵循总文档的已确认需求；新增技术方案以“补全建议”提供，执行时不得冒充用户原话。
- 先更新对应模块文档，再实现代码。不得修改本总计划以外无关需求、不得代用户审批。
- 共享文件由总负责人分配；遇到权限、状态或契约变更需求提交给核心负责人，不同时覆盖他人文件。
- 每完成一个步骤更新本任务；由总负责人同步总计划的步骤统计，避免多 Agent 同写总文件。
- 最终回归发现问题时在父计划登记新的修正小任务，不恢复已删除任务，也不改写原完成摘要。

## 已完成事实与执行记录

仅完成本任务文档准备；产品实现、定向验证和交接均未开始。

| 项目          | 记录                         |
| ------------- | ---------------------------- |
| 执行 Agent    | 未分配                       |
| 开始/结束时间 | 未开始                       |
| 采用契约版本  | 未接收                       |
| 实际修改文件  | 无                           |
| 定向检查结果  | 未执行                       |
| 未解决问题    | 无已执行发现；前置条件见依赖 |
| 交接摘要      | 未交接                       |

---

## 评审调研结论（2026-09-12 整体评审落位）

> 来源：[整体评审与强化方案](review/design-review.md)、[可行性缺陷分析](review/feasibility-findings.md)、[技术栈调研](review/tech-stack-research.md)、[渲染栈聚焦调研](review/rendering-stack-research.md)。本节是评审产出的执行提示，只补充信息，不修改本任务既有范围、步骤与验收标准；标注 U-xx 的事项未经用户裁定前不得视为已确认需求。

### 1. 方案建议与调研结论建议
- 模板数据与渲染适配器保持单向依赖：C16-scene-v1 模板是纯数据（词表＋区域布局），与渲染引擎解耦，S05 只填数据、不改 schema／布局算法／Canvas 引擎（t2 领域 4 §4.2d；渲染栈 §6.2）。
- 区域内相对坐标落位须支持 zoom 重排：统一 zoom 因子下 DOM 与 Canvas 同乘同一因子，区域尺寸变化后人物按区域相对坐标重排（t2 领域 4 §4.2c；渲染栈 §6.2）。

### 2. 可能存在的问题点
- 落位／重排算法无现成组件：panzoom 是「库」非「组件」（≈7k，仍需自接 DOM/canvas），区域尺寸变化后的重排属场景模板数据驱动布局的自写部分，不要误以为选库即得（渲染栈 §6.2）。

### 3. 优化建议
- 增-15 ④：平移缩放采用 CSS transform 统一 zoom 因子＋@vueuse 手势 composable（零新依赖），panzoom 作即用备选、d3-zoom 不推荐（增-15 ④；渲染栈 §6）。
- 区域尺寸变化后按相对坐标重排列入增-15 自写 composable 清单（「场景模板落位」「zoom 重排」两项与 S05 模板数据直接相关），随 C16 引擎联调（增-15 依赖与体积段；渲染栈 §6.2）。
- 模板格式 C16-scene-v1 保持不变：增-15 明确不改变 D14/D15 分层架构与「业务不等待动画」约束，四类模板仅按既有字段与区域约束填充（design-review §5 增-15）。
