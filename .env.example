# cheryClaw 用户配置（占位符：API Key 等）
# 实际生效文件是 .env，本文件是模板，不会被后端加载。

# .chery 配置根目录：
# - 留空（推荐）：默认指代 cheryClaw.exe 所在目录，自动推断。
# - 自定义：取消注释并填写绝对路径，例如 D:\cheryClaw\.chery
#   注意：所填路径必须是 cheryClaw.exe 同级的 .chery/ 目录，否则后端读不到配置。
# CHERY_DIR=

# config.yaml 中 $KEY 占位符替换：这里不写也行，可在系统环境变量里写
# 优先级：运行环境 path > 本文件
LONGCAT_API_KEY=your-longcat-api-key