# CheryNyxus 用户配置（占位符：API Key 等）
# 实际生效文件是 .env，本文件是模板，不会被后端加载。

# .chery 配置根目录：
# - 留空（推荐）：默认指代 CheryNyxus.exe 所在目录，自动推断。
# - 自定义：取消注释并填写绝对路径，例如 D:\CheryNyxus\.chery
#   注意：所填路径必须是 CheryNyxus.exe 同级的 .chery/ 目录，否则后端读不到配置。
# CHERY_DIR=

# 会话签名密钥 CHERY_AUTH_SESSION_SECRET：后端启动时自动生成并写入 .env，勿手工填写；
# 删除该行后重启即重新生成（已签发登录 token 全部失效需重新登录）。
# .env 是唯一 env 文件；.chery/ 下不再存放 .env。

# config.yaml 中 $KEY 占位符替换：这里不写也行，可在系统环境变量里写
# 优先级：运行环境 path > 本文件
LONGCAT_API_KEY=your-longcat-api-key